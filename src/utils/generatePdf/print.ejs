<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes da Reserva</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            /* Adicionando margem nos quatro cantos do body */
            padding: 0;
        }

        h1,
        h2,
        h3 {
            margin: 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            padding: 8px 12px;
            border: 1px solid #ccc;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        .summary {
            margin-top: 20px;
        }

        /* Adicionando margem nos quatro cantos do contêiner principal */
        div {
            margin: 20px;
            /* Você pode ajustar o valor da margem conforme necessário */
        }
    </style>
</head>

<body>
    <div>
        <h1>Detalhes da Reserva</h1>
        <h3>Nome do Usuário: <%= reservationData.userName %>
        </h3>
        <h3>Hotel: <%= reservationData.hotel.name %> - <%= reservationData.hotel.location %>
        </h3>
        <h3>Check-In: <%= new Date(reservationData.checkInDate).toLocaleDateString() %>
        </h3>
        <h3>Check-Out: <%= new Date(reservationData.checkOutDate).toLocaleDateString() %>
        </h3>

        <table>
            <thead>
                <tr>
                    <th>Número do Quarto</th>
                    <th>Tipo de Quarto</th>
                    <th>Preço por Noite</th>
                </tr>
            </thead>
            <tbody>
                <% reservationData.rooms.forEach(room=> { %>
                    <tr>
                        <td>
                            <%= room.roomNumber %>
                        </td>
                        <td>
                            <%= room.roomType %>
                        </td>
                        <td>R$ <%= room.pricePerNight.toFixed(2) %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>

        <div class="summary">
            <h3>Total: R$ <%= reservationData.totalCost.toFixed(2) %>
            </h3>
            <h3>Status: <%= reservationData.status %>
            </h3>
        </div>
    </div>
</body>

</html>